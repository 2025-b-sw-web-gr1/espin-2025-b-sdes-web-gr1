<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>{{titulo}}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #f4f4f4;
            text-align: center;
        }

        h1 {
            margin-top: 30px;
        }

        .menu {
            margin-top: 40px;
        }

        .item {
            background: white;
            padding: 20px;
            margin: 15px auto;
            width: 300px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }

        a {
            text-decoration: none;
            font-size: 22px;
            color: #16a085;
            font-weight: bold;
        }
    </style>
</head>
<body>

    <h1>{{titulo}}</h1>

    <div class="menu">
        {{#each categorias}}
            <div class="item">
                <a href="/categoria/{{ruta}}">{{nombre}}</a>
            </div>
        {{/each}}
    </div>

</body>
</html>
